// firebase.js — DEPRECATED
// Firebase SDK was removed from this project. Keep this placeholder only so the file path doesn't break if referenced.
// If you plan to delete this file entirely, remove it and update any references elsewhere.

export default null;
